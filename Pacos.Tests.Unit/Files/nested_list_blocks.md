Nested list with various block types:

- First item with a quote inside:
    - Nested item with blockquote:
        > This is a quote inside a nested list.
        > It has two lines.
    - Another nested item after the quote.
- Second item with code block inside:
    - Here comes some code:
        ```python
        def hello():
            print("world")
        ```
    - And text after the code.
- Third item mixes everything:
    - A quote first:
        > Mixing quotes and code in nested lists.
    - Then code:
        ```
        some code block
        ```
    - And a final text item.
